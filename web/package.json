{
  "scripts": {
    "start:parcel": "parcel index.html",
    "start:db": "json-server -w db.json",
    "start": "concurrently 'yarn start:parcel' 'yarn start:db'"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "json-server": "^0.16.1",
    "parcel-bundler": "^1.12.4",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "eventing": "^0.1.0"
  }
}
